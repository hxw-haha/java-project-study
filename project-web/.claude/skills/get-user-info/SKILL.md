---
name: get-user-info
description: 查询用户基本信息
tags: [user, query]
version: 1.0.0
mcp-server: dubbo-mcp-server   # 可选，指定要使用的 MCP Server
---

# 查询用户信息

直接调用 `get_user_info` 工具，传入 userId 即可。
返回用户信息后，用友好方式展示给用户。